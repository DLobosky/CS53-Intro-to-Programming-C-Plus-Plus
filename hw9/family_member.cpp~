//Programmer: Keith Loveless                    Date: 19 April 2013
//File: family_member.cpp                       Class: CS 53 A
//Purpose: defines the house class

#include "family_member.h"
#include <fstream>
#include <string>

using namespace std;

family_member::family_member()
{
  ifstream fin;
  fin.open("family.dat");
  if(fin.is_open())
  { 
    for(int i = 0; i <= rand()%4; ++i)
      fin >> m_name >> m_slop;
  }
  else if(m_slop > 100)
    exit(4);
  else
    cout << "The file cannot be opened" << endl;
  cout << m_name << m_slop;
  set_row(0);
  set_col(0);
}

string family_member::get_name()
{
  return m_name;
}

unsigned short family_member::get_row()
{
  return m_row;
}

unsigned short family_member::get_col()
{ cout << m_col;
  return m_col;
}
/*void family_member::set_name(const string &name)
{
  m_name = name;
  return;
}*/

void family_member::set_row(const int row)
{
  m_row = row;
  return;
}

void family_member::set_col(const int col)
{
  m_col = col;
  return;
}

ostream &operator<<(ostream& out, const family_member &fm)
{
  out << fm.m_name << " is at < " << fm.m_row << ", " 
      << fm.m_col << " > has slopiness " << fm.m_slop << endl;
  return out;
}
